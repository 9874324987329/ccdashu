<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.chinaredstar.perseus.db.dao.UserMapper">

    <resultMap id="TestUsertMap" type="com.chinaredstar.po.TestUserPo">
        <id column="id" property="id" jdbcType="INTEGER"/>
        <result column="name" property="name" jdbcType="VARCHAR"/>
        <result column="mobile" property="mobile" jdbcType="VARCHAR"/>
    </resultMap>

    <sql id="search_condition">
        <if test="true">
            and name like CONCAT('%','${condition.name}','%' )
        </if>
    </sql>

    <select id="listTestUserCount" parameterType="com.chinaredstar.common.Search" resultType="int">
        select count(1) from test_user where 1=1
        <include refid="search_condition"/>
    </select>

    <select id="listTestUser" parameterType="com.chinaredstar.common.Search" resultMap="TestUsertMap">
        select * from test_user where 1=1
        <include refid="search_condition"/>
        limit #{start},#{pageSize}
    </select>

    <select id="selectTestUserByID" parameterType="int" resultMap="TestUsertMap">
		select * from test_user where id=#{id}
	</select>

    <update id="updateTestUser" parameterType="com.chinaredstar.po.TestUserPo">
		update test_user set name=#{name},mobile=#{mobile} where id=#{id}
	</update>
    <insert id="insertTestUser" parameterType="com.chinaredstar.po.TestUserPo">
		insert into test_user(name,mobile) values(#{name},#{mobile})
	</insert>


    <delete id="deleteTestUser" parameterType="int">
		delete from test_user where id=#{id}
	</delete>

</mapper>